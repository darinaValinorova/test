function Summator (num) {
	var sum (numOne, numTwo){
		return numOne + numTwo;
	}
	var run (){
		var twoNumber = prompt ('Введите два числа.', '100 50');
		var arr = twoNumber.split(' ');
		var result = sum(arr(0), arr(1));
	}
}

var num = new Summator (50);
num.sum ()
//что-то в этой жизни пошло не так